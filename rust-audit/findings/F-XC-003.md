# F-XC-003 `deny_unknown_fields` is combined with `#[serde(flatten)]` in the validator and sentinel configs, and no test proves a mistyped key is rejected

| Field                | Value                                                                          |
| -------------------- | ------------------------------------------------------------------------------ |
| Status               | QA-done                                                                      |
| Crate and module     | cross-cutting: `validator/config.rs`, `sentinel/config.rs`, `core/index/mod.rs` |
| Location             | `crates/validator/src/config.rs:20-39`; `crates/sentinel/src/config.rs:17-39`; `crates/core/src/index/mod.rs:20-29` (contrast: `crates/sentinel-engine/src/config.rs:19-32` and its test at `:129-144`) |
| Severity             | Low / Low                                                                      |
| Certainty            | 58%                                                                            |
| Assumptions involved | A1                                                                             |
| Tags                 | config                                                                         |

## Claim

Three configuration structs put `#[serde(deny_unknown_fields)]` on a container that also has a
`#[serde(flatten)]` field. Serde documents that combination as unsupported. The observable
consequence, if `deny_unknown_fields` is inert there, is that a mistyped key in a validator or
sentinel TOML is silently accepted and the intended setting never applies — including the two
settings whose absence is a security-relevant degradation rather than a cosmetic one:

- `[index] use_client_filtering` — the switch that makes the event watcher verify `eth_getLogs`
  results client-side instead of trusting the node's filtering (A4, CORE-H2). A typo here leaves
  the validator running with node-filtered, unverified results while the operator believes the
  protection is on.
- `[index] max_reorg_depth` — a typo leaves the built-in default in place instead of the
  operator's chosen depth (A5).

Both live inside `index::Config`, which is itself a `deny_unknown_fields`-plus-two-`flatten`
container (`core/index/mod.rs:20-29`), so the same question applies one level down.

What is *not* in doubt, and is the load-bearing half of this finding: **the engine, which is the
only one of the three configs with no flattened field, is also the only one with a test proving
unknown keys are rejected.** The two configs that use `flatten` have `parses_sample_config` and
missing-field tests but no unknown-field test, so nothing in the suite would notice if the
attribute were inert.

## Basis

| # | Claim | Class | Citation | Verbatim quote |
| - | ----- | ----- | -------- | -------------- |
| 1 | The validator config denies unknown fields and flattens the driver config into the same container | E2 | `crates/validator/src/config.rs:20-22`, `:36-39` | ```#[derive(Debug, Deserialize)]```<br>```#[serde(deny_unknown_fields)]```<br>```pub struct Config {```<br>```    /// Configuration for the service driver and its components.```<br>```    #[serde(default, flatten)]```<br>```    pub driver: driver::Config,```<br>```}``` |
| 2 | The sentinel config does the same | E2 | `crates/sentinel/src/config.rs:17-19`, `:36-39` | ```#[derive(Debug, Deserialize)]```<br>```#[serde(deny_unknown_fields)]```<br>```pub struct Config {```<br>```    /// Configuration for the service driver and its components.```<br>```    #[serde(flatten)]```<br>```    pub driver: driver::Config,```<br>```}``` |
| 3 | `index::Config` repeats the pattern with two flattened fields | E2 | `crates/core/src/index/mod.rs:20-29` | ```#[derive(Clone, Debug, Default, Deserialize, PartialEq, Eq)]```<br>```#[serde(default, deny_unknown_fields)]```<br>```pub struct Config {```<br>```    /// Block watcher configuration.```<br>```    #[serde(flatten)]```<br>```    pub blocks: blocks::Config,```<br>```    /// Event watcher configuration.```<br>```    #[serde(flatten)]```<br>```    pub events: events::Config,```<br>```}``` |
| 4 | The intermediate `driver::Config` does *not* deny unknown fields, so anything routed into it is ignored | E2 | `crates/core/src/driver.rs:30-37` | ```#[derive(Clone, Debug, Default, Deserialize, PartialEq)]```<br>```#[serde(default)]```<br>```pub struct Config {```<br>```    /// Indexer (block and event watcher) configuration.```<br>```    pub index: index::Config,```<br>```    /// Transaction queue configuration.```<br>```    pub transactions: tx::Config,```<br>```}``` |
| 5 | Only the engine — the config with no `flatten` — has an unknown-field test | E2 | `crates/sentinel-engine/src/config.rs:129-135` | ```    #[test]```<br>```    fn rejects_unknown_field {```<br>```        assert!(```<br>```            toml::from_str::<Config>(```<br>```                r#"```<br>```                    rpc = "https://eth.llamarpc.com"```<br>```                    invalid_field = "foo"``` |
| 6 | No such test exists for the validator or the sentinel | E2 | — | `grep -rn --include='*.rs' 'unknown_field\|invalid_field\|rejects_unknown' crates/` returns eleven `#[serde(deny_unknown_fields)]` attribute lines plus exactly one test, `crates/sentinel-engine/src/config.rs:130`. |
| 7 | Serde documents `deny_unknown_fields` as unsupported together with `flatten` | I | — | Serde's own attribute documentation; the dependency source is not on disk (baseline §1, A6), so the runtime behaviour of this specific combination in the locked `serde 1.x` cannot be established here. |
| 8 | The affected setting is a real integrity switch | E2 | `crates/validator/validator.sample.toml:66-70` | ```[index]```<br>```# Optional: set to work around RPC providers that unreliably return```<br>```# `eth_getLogs` results (see the validator handbook's "`eth_getLogs````<br>```# Reliability" section).```<br>```# use_client_filtering = true``` |

## Trigger

An operator copies `validator.sample.toml`, uncomments the `[index]` block and writes
`use_client_filtering = ture` (or `use_client_filter`, or puts it under `[indexer]`). If
`deny_unknown_fields` is inert on this container the file loads without complaint, the validator
starts, `/health` answers OK, and the client-side `eth_getLogs` verification is off. Nothing in
the logs distinguishes this from a correct configuration: the setting has no startup log line and
no metric. The same shape applies to `max_reorg_depth` and to every key under `[transactions]`.

## Considered and rejected

- **"It is a compile error, so the code would not build."** I could not resolve this without a
  toolchain, and the honest reading of the available evidence is that it is not: the workspace
  builds in CI (`Justfile:29`, `:44`) with the attribute pair present in three structs, and a
  coverage report was posted on PR #896 (`codebase-map.md:59`). So the combination is accepted by
  the locked `serde_derive`; what it *does* at runtime is the open question. This is stated as
  class `I` rather than asserted either way.
- **"The samples do not parse, so this is moot."** They do parse — each crate has a
  `parses_sample_config` test reading the real file from `CARGO_MANIFEST_DIR`
  (`crates/validator/src/config.rs:282-289`, `crates/sentinel/src/config.rs:136-143`,
  `crates/sentinel-engine/src/config.rs:146-153`). Sample-vs-schema drift is genuinely covered.
  That is exactly why the *unknown*-key case stands out as the one gap.
- **"Unknown fields would be caught by the inner struct's own `deny_unknown_fields`."** For the
  `[index]` and `[transactions]` tables the inner structs do deny (`core/index/mod.rs:21`,
  `core/tx/mod.rs:70`, `core/index/blocks.rs:48`, `core/index/events.rs:72`), but they are only
  reached if the flattened routing delivers the key there in the first place; and
  `driver::Config` in between denies nothing (basis row 4), so a stray *top-level* key or a stray
  `[table]` has no denier on its path at all.
- **"`observability` is affected too."** It is not: it is a named field, not flattened, and
  `observability::Config` denies unknown fields itself (`core/observability/mod.rs:17`).
- **Severity.** Low, not higher: under A1 the operator is trusted and this is a
  misconfiguration-detection gap, not an attacker-reachable path. It is filed because the
  degradation is silent and because the fix is one test.

## Remediation options

1. Add the missing test to both configs — the engine's `rejects_unknown_field` copied verbatim
   with a validator/sentinel body, plus one that plants a typo *inside* `[index]`. If the tests
   pass, the concern is closed by evidence and the tests keep it closed; if they fail, the bug is
   real and was found for the price of ten lines.
2. If they fail: drop `flatten` and make `driver` a named `[driver]` table (a breaking config
   change, but it restores the denial and removes the ambiguity), or move `deny_unknown_fields`
   down to every non-flattened leaf and accept that top-level typos are not caught.
3. Independently of the above, log the resolved configuration at startup (with secrets redacted,
   see F-XC-002) so an operator can see that `use_client_filtering` is off. Today the only
   startup line is `config_file = %options.config_file.display`
   (`crates/validator/src/main.rs:43`), which says the file was read but nothing about what it
   contained.

## Trail

- Reviewer R10: drafted, self-estimate 55% (the attribute pairing, the missing tests
  and the engine contrast are E2 from direct reads; the runtime behaviour of `deny_unknown_fields`
  under `flatten` is `I` and cannot be settled without a toolchain, A9 false)

## Critic (C-XC)

### Per-claim verdicts

| # | Verdict | Note |
| - | ------- | ---- |
| 1 | **Supported** | `validator/src/config.rs:20-22` is `#[derive(Debug, Deserialize)] / #[serde(deny_unknown_fields)] / pub struct Config {`, and `:36-38` is `#[serde(default, flatten)] pub driver: driver::Config,`. |
| 2 | **Supported** | `sentinel/src/config.rs:17-19` and `:35-37` — same shape, `#[serde(flatten)]` without `default`. |
| 3 | **Supported** | `core/src/index/mod.rs:20-29` verbatim; **two** flattened fields under one `deny_unknown_fields`. |
| 4 | **Supported, and this is the strongest row in the table** | `core/src/driver.rs:29-37` — `#[serde(default)]` with no `deny_unknown_fields`. See below. |
| 5 | **Supported** | `sentinel-engine/src/config.rs:129-144`. |
| 6 | **Supported** | I re-ran the grep verbatim: eleven `#[serde(deny_unknown_fields)]` attribute lines plus exactly one test, at `sentinel-engine/src/config.rs:130`. Reproduced. |
| 7 | **Supported as `I`, and correctly classed** | Serde's semantics for `deny_unknown_fields` + `flatten` cannot be established here: no dependency source is on disk (baseline §1) and no toolchain exists (A9 false). Marking this `E2` would have been `H`; R10 did not. |
| 8 | **Supported** | `validator.sample.toml:66-70` verbatim. |

No claim is marked `H`.

### Being precise about `E2`-from-this-repo versus `I`-about-serde, as instructed

**`E2` from this checkout, no serde knowledge required:**

1. The attribute pair exists in three structs (rows 1-3).
2. `driver::Config` — the container everything flattened is routed *into* — has **no**
   `deny_unknown_fields` (row 4). Whatever serde does with the outer attribute, an unknown key
   that reaches this struct is dropped in silence. That is a repo-internal fact and it is the
   half of the finding that does not depend on upstream semantics at all.
3. No test anywhere asserts that a validator or sentinel config rejects an unknown key (row 6).
4. **The combination compiles and the flatten routing works.** `validator/src/config.rs:235-275`
   (`deserializes_with_optional_service_fields`) deserializes a TOML document containing
   `[index] max_reorg_depth = 12` and `[transactions] max_in_flight_transactions = 4` and asserts
   `config.driver.index.blocks.max_reorg_depth == 12` and
   `config.driver.transactions.max_in_flight_transactions == 4`. That is a doubly-flattened path
   (outer `Config` → `driver::Config` → `index::Config` → `blocks::Config`) exercised by a test in
   the tree. This is stronger evidence for R10's "it is not a compile error" than the
   CI-and-coverage-report argument the Considered-and-rejected section actually uses, and it comes
   from the repository rather than from a document about it.

**`I`, and unresolvable this run:** whether `serde_derive` at the locked version honours,
silently drops, or partially applies `deny_unknown_fields` on a container that also has a
`flatten` field — and correspondingly whether a stray *top-level* key in `validator.toml` is
rejected or ignored. Nothing in this checkout answers that, and I decline to answer it from
recollection of upstream documentation.

### What this means for the finding's shape

The finding is really two claims of different strength, and the file presents them as one:

- **A test gap (`E2`, certain).** Two of the three service configs have no unknown-field test, and
  the third — the one that *has* the test — has no flattened field, so its test proves nothing
  about the interaction. R10 states this accurately ("the only one of the three configs with no
  flattened field"), but a reader skimming the Claim could take the engine as the crate that
  "got it right" on this specific question. It is simply a different shape.
- **A possible silent-acceptance bug (`I`).** Conditional on serde's behaviour.

Both point at the same one-line remediation, so the finding stands as filed.

### Finding verdict

**Plausible.** The mechanism is verified — the attribute pair, the un-denying intermediate
container, the missing tests — but the consequence the Claim leads with ("a mistyped key is
silently accepted") rests on an upstream behaviour that is class `I` here, and the trigger is an
operator typo rather than an observed failure.

**Certainty: 58%.** Mid-band: everything checkable is checked and correct; the one link that
decides whether this is a bug or a non-issue is unreadable this run.

**Severity: Low (unchanged).** Under A1 the operator is honest but not infallible, and the two
settings named — `use_client_filtering` (an A4 integrity switch) and `max_reorg_depth` (A5) —
degrade silently if a typo is swallowed. That is "configuration weakness with limited impact",
which is Low. Not Informational, because the failure is undetectable at runtime: there is no
startup log of the resolved configuration (`validator/src/main.rs:43` logs only the file path) and
no metric.

### Note for QA

Remediation option 1 is the whole finding: add `rejects_unknown_field` to
`crates/validator/src/config.rs` and `crates/sentinel/src/config.rs`, plus one that plants the typo
*inside* `[index]`. Ten lines, and the result is decisive either way — a pass closes the `I` leg
permanently and a failure turns this into a Confirmed Medium. QA should write these tests even
though they cannot be run this session, so the first person with a toolchain gets the answer for
free.

## QA (QA-XC)

**Not attempted (no toolchain).** Nothing was executed (A9 FALSE).

**PoC written:** `rust-audit/poc/F-XC-003/` — six tests, three for
`crates/validator/src/config.rs` and three for `crates/sentinel/src/config.rs`, both to be pasted
into the `mod tests` block those files already have. This is C-XC's "Note for QA" in full,
including the typo planted *inside* `[index]`.

**What would be run and what it would show.** `cargo test -p validator config::tests::qa_xc_003`
and `cargo test -p sentinel config::tests::qa_xc_003`. The cases are: an unknown top-level key; a
typo in `[index] use_client_filtering` (the A4 integrity switch); a mistyped `[observabilty]`
table; a typo in `[index] max_reorg_depth` reached through two levels of flatten; and — as the
control — a typo inside `[sentinel]`, a struct with `deny_unknown_fields` and **no** flattened
field, which must reject whatever serde does with the outer container. A green run closes this
finding's `I` leg permanently and drops it to Informational; a red run makes it Confirmed, and
**Low → Medium** is then defensible because the failure is undetectable at runtime
(`crates/validator/src/main.rs:43` logs only the config file path, there is no startup echo of
the resolved configuration, and no metric).

Two notes for whoever runs them. First, the two crates flatten differently — the validator uses
`#[serde(default, flatten)]` (`crates/validator/src/config.rs:36-38`), the sentinel a bare
`#[serde(flatten)]` (`crates/sentinel/src/config.rs:35-37`) — so a *disagreement* between the two
results is itself an answer and must not be read as a flaky test. Second, in the sentinel test the
stray top-level key is inserted **before** the first table header rather than appended to the
fixture, because appending would place it inside `[sentinel]` and test the wrong thing.

**Remediation check — option 1 is sound but is a detector, not a fix.** The finding's own
remediation is "add the tests", which this PoC is. If they fail, the fix I would take is one the
finding does not list: add `#[serde(deny_unknown_fields)]` to `core::driver::Config`
(`crates/core/src/driver.rs:30`), matching all five of its siblings in `core`. That closes the
half of the defect that does not depend on serde's behaviour at all — the half C-XC correctly
identifies as the strongest row in the table — and it is one line. Dropping the flatten entirely
would also work but is a breaking configuration change for every deployed operator and would
require rewriting the handbooks. Echoing the fully resolved configuration at `info` on startup
does not prevent the typo but makes it visible, and is the same remediation `F-ENG-009` asks for
in the engine; it is worth doing regardless of the outcome.

**Certainty: unchanged at 58%.** I add no new evidence and the question stays `I` until the tests
run. It is question 3 of `rust-audit/poc/UNRESOLVED-DEPENDENCY-QUESTIONS.md`, five minutes.

## Verification (V-XC, Phase 5)

**Outcome: the pivotal `I` leg is closed, and it closes in the finding's favour-of-the-code
direction. `deny_unknown_fields` is honoured despite the flatten, on both crates and at every
depth tested.**

### What I ran

QA-XC's six ready tests from `rust-audit/poc/F-XC-003/`, installed by appending each file inside
the existing `#[cfg(test)] mod tests` block of its target, run, then reverted with
`git checkout --`. No test body was altered; no mechanical repair was needed — both files compiled
as written on the first attempt.

```
$ cargo test -p validator config::tests::qa_xc_003 --locked
running 3 tests
test config::tests::qa_xc_003_rejects_a_mistyped_table_name ... ok
test config::tests::qa_xc_003_rejects_an_unknown_top_level_key ... ok
test config::tests::qa_xc_003_rejects_a_typo_in_a_security_relevant_key ... ok

test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 38 filtered out; finished in 0.00s
```

```
$ cargo test -p sentinel config::tests::qa_xc_003 --locked
running 3 tests
test config::tests::qa_xc_003_rejects_a_typo_inside_the_sentinel_table ... ok
test config::tests::qa_xc_003_rejects_an_unknown_top_level_key ... ok
test config::tests::qa_xc_003_rejects_a_typo_in_a_flattened_driver_key ... ok

test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 37 filtered out; finished in 0.00s
```

**All six pass.** This is the first row of the PoC README's outcome table.

### What that settles

Question 3 of `rust-audit/poc/UNRESOLVED-DEPENDENCY-QUESTIONS.md` is answered: **yes**,
`#[serde(deny_unknown_fields)]` does something on a container that also carries
`#[serde(flatten)]`. Serde routes the unknown key into the flattened container's map, and the
outer container's deny still fires. Concretely, and in the terms the question asked:

- **A stray top-level key is rejected**, on both crates. `not_a_real_field = "typo"` errors.
- **A typo inside `[index]` is rejected** — `use_client_filterring` on the validator (the **A4**
  client-side-filtering switch) and `max_reorg_dept` on the sentinel (the **A5** knob). These are
  the sharp cases the question named by name, and both fail loudly rather than degrading in
  silence.
- **A mistyped table name is rejected**: `[observabilty]` errors.
- **The two crates do not disagree.** The validator's `#[serde(default, flatten)]` and the
  sentinel's bare `#[serde(flatten)]` behave identically here, so no fix has to straddle both
  forms.
- The control passes: a typo inside `[sentinel]`, whose container has no flattened field, errors
  as it must.

**The one fact that survives regardless is untouched and still true**, and it is worth keeping in
the finding: `core::driver::Config` is `#[serde(default)]` with **no** `deny_unknown_fields`
(`crates/core/src/driver.rs:29-37`), unlike all five of its siblings in `core`. The six tests show
that the *outer* deny catches keys before they get there, so today nothing reaches it — but the
asymmetry is real, it is one line to fix, and it is what would break if any of these containers
ever lost its own `deny_unknown_fields`.

### Verdict

**Basis class: the pivotal leg moves from `I` to `E1`** — executed, six tests, verbatim output
above.

**Severity: Low / Low → Informational / Informational.** The finding's escalation path to
Low→Medium was explicitly conditioned on a test failing ("the failure is undetectable at runtime").
None failed. What remains is exactly what the finding's first outcome row describes: a **test
gap** — the two crates that combine `deny_unknown_fields` with `flatten` had no test proving the
combination works, and `sentinel-engine`'s green `rejects_unknown_field` proved nothing about it
because that config has no flattened field. The gap is real; the defect is not.

**Certainty: 58% → 96%.** Executed, deterministic, no network, no fixtures beyond literals in the
test bodies. The 4% is scope, not doubt: six inputs are not a proof over every nesting the schema
admits.

**Remediation: option 1 only, and it is now free.** Land QA-XC's six tests as-is — they are the
regression guard for a serde behaviour the repository depends on but does not pin, and they cost
nothing to keep. Option 1 of the finding *is* the remediation and it is sound. Options 2 (drop the
flatten) and 3 (echo the resolved config at startup) are no longer justified by this finding;
option 3 remains independently worth doing for `F-ENG-009`'s reasons, not this one's. Adding
`deny_unknown_fields` to `core::driver::Config` to match its five siblings is still a defensible
one-line consistency fix, but it is now hygiene rather than a repair.
