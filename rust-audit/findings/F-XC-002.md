# F-XC-002 Secret-bearing types reach log statements through derived `Debug`; the redaction policy is inconsistent and nothing enforces it

| Field | Value |
| --- | --- |
| Status | Verified (reduced — the leak is refuted) |
| Crate and module | cross-cutting: `core/effects.rs`, `core/driver.rs`, `validator/service/effect.rs`, `validator/frost/keygen.rs` |
| Location | `crates/core/src/effects.rs:55`, `:59`, `:78`; `crates/core/src/driver.rs:238`, `:261`; `crates/validator/src/service/effect.rs:249`; `crates/validator/src/frost/keygen.rs:27-32`, `:433-435` (contrast: `crates/validator/src/frost/ecdh.rs:50-53`, `crates/validator/src/frost/preprocess.rs:64-70`, `:150`, `crates/core/src/tx/signer.rs:96-100`) |
| Severity | Low / Low |
| Certainty | 88% (V-VAL, Phase 5 — leak leg executed and REFUTED; hygiene leg verified) |
| Assumptions involved | A1, A6 |
| Tags | crypto, secrets, observability |

## Claim

The repository has a secret-redaction convention — four types hand-write a `Debug` impl that prints `"redacted"` instead of the key material — but the convention is applied by hand, is applied to exactly the types whose secret lives in a _local_ field, and is _not_ applied to the two types whose secret lives inside a `frost-core` container. Those two types, `Secrets` and `KeyShare`, `#[derive(Debug)]`, and both are reachable from a `tracing` statement:

- `KeyShare` is carried by `Effect::StartNonceGeneration` and `Effect::ReconcileGroupSecrets`. `crates/validator/src/service/effect.rs:249` formats the whole `Effect` with `?effect` at **`warn!`**, which the default `log_filter = "info"` emits. Any effect failure — a transient SQLite error during `ReconcileGroupSecrets` is the cheapest one — writes every tracked `KeyShare` through `Debug` into the log at the default verbosity.
- `Secrets` (the DKG encryption key, the FROST round-1 secret package and the proof of knowledge) is carried by `Resume::Setup`. `crates/core/src/effects.rs:59` and `:78` format every resume with `?resume` at `trace!`, and `crates/core/src/driver.rs:261` does it a third time. The same generic sites also format every `Effect` (`effects.rs:55`) and every `Update` (`driver.rs:238`).

The `core` sites are the cross-cutting part: they are generic over the service's own `Effect` and `Resume` types and therefore impose no constraint on what those types print. `core` requires only `Effect: Debug` and `Resume: Debug` (`effects.rs:40-41`) and then logs them; whether that is safe is decided entirely in the downstream crate, with no compile-time or review-time gate. The validator is the crate that puts secrets in those types.

Whether the bytes actually appear is decided by `frost-core`'s own `Debug` impls for `round1::SecretPackage` and `keys::KeyPackage`. **That is not verifiable in this checkout** — no dependency source is on disk (baseline §1, A6) — so the leak itself is class `I` and the finding is written as "the repo's own hygiene has a hole and depends on an upstream property nobody here can check", not as "the key is printed".

## Basis

| # | Claim | Class | Citation | Verbatim quote |
| --- | --- | --- | --- | --- |
| 1 | `core` logs every effect and every resume through `Debug`, generically | E2 | `crates/core/src/effects.rs:54-60` | `    pub fn spawn(&mut self, effect: Effect) {`<br>`        tracing::trace!(?effect, "spawning effect task");`<br>`        let handler = Arc::clone(&self.handler);`<br>`        self.tasks.spawn(async move {`<br>`            let resume = handler.perform_effect(effect).await;`<br>`            tracing::trace!(?resume, "effect task finished");`<br>`            resume` |
| 2 | A third generic resume log site | E2 | `crates/core/src/effects.rs:77-79` | `                Some(Ok(resume)) => {`<br>`                    tracing::trace!(?resume, "effect resume collected");`<br>`                    return resume;` |
| 3 | The validator logs the whole `Effect` at `warn!`, i.e. at the default level | E2 | `crates/validator/src/service/effect.rs:246-252` | `        let (resume, result) = match self.try_perform_effect(effect.clone).await {`<br>`            Ok(resume) => (resume, EffectResult::Success),`<br>`            Err(err) => {`<br>`                tracing::warn!(?effect, %err, "failed to perform effect");`<br>`                (Resume::Noop, EffectResult::Failure)`<br>`            }`<br>`        };` |
| 4 | The default log filter is `info`, so `warn!` is emitted out of the box | E2 | `crates/core/src/observability/mod.rs:30-35` | `    fn default -> Self {`<br>`        Self {`<br>`            log_filter: EnvFilter::new("info"),`<br>`            metrics_address: SocketAddr::from((Ipv4Addr::LOCALHOST, 0)),`<br>`        }`<br>`    }` |
| 5 | `Secrets` derives `Debug` and holds the DKG secret package | E2 | `crates/validator/src/frost/keygen.rs:27-32` | `#[derive(Clone, Debug, Deserialize, Serialize)]`<br>`pub struct Secrets {`<br>`    encryption_key: EncryptionKey,`<br>`    secret_package: round1::SecretPackage,`<br>`    proof_of_knowledge: Signature,`<br>`}` |
| 6 | `KeyShare` derives `Debug` and is a transparent wrapper over the FROST key package | E2 | `crates/validator/src/frost/keygen.rs:433-435` | `#[derive(Clone, Debug, Deserialize, Serialize)]`<br>`#[serde(transparent)]`<br>`pub struct KeyShare(keys::KeyPackage);` |
| 7 | The repo _does_ know how to redact — it does so for the locally-held secrets | E2 | `crates/validator/src/frost/ecdh.rs:50-53` | `impl Debug for EncryptionKey {`<br>`    fn fmt(&self, f: &mut Formatter) -> fmt::Result {`<br>`        f.debug_tuple("EncryptionKey").field(&"redacted").finish`<br>`    }` |
| 8 | …and again for the signing nonces | E2 | `crates/validator/src/frost/preprocess.rs:64-70` | `impl Debug for Nonces {`<br>`    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {`<br>`        f.debug_struct("Nonces")`<br>`            .field("signing_nonces", &"<redacted>")`<br>`            .field("proof", &self.proof)`<br>`            .finish`<br>`    }`<br>`}` |
| 9 | Whether `round1::SecretPackage` / `keys::KeyPackage` redact is an upstream property | I | baseline `rust-audit/state/baseline.md:62-65` | ``**Dependency sources are also absent** (relevant to assumption A6): there is no `vendor/`, no``<br>````target/`, no `.cargo/`in the repo and no`*/cargo/registry`directory anywhere under a```<br>```4-level scan of`/`. No dependency source (`frost-core`, `frost-secp256k1`, `alloy`, `sqlx`,``` |

## Trigger

Concrete, at the default configuration (`log_filter = "info"`, as shipped in all three sample TOMLs): the state machine emits `Effect::ReconcileGroupSecrets { groups }` carrying the current `Arc<KeyShare>` for each retained group; the handler's SQLite write fails once (disk full, the file locked by a concurrent backup — `docs/validator-handbook.md` recommends backups); the `Err` arm at `service/effect.rs:249` fires and formats the whole `Effect`, key shares included, into the process log. Under A1 the log file is on a trusted host, but logs are the artefact most likely to be shipped to an aggregator, attached to a bug report, or read by an operator who is not entitled to the share.

For `Secrets` the trigger additionally requires `log_filter` to include `trace` for the `safenet_core` target, which the handbooks present as a normal debugging step.

## Considered and rejected

- **"`frost-core` redacts, so nothing leaks."** Possible, and it is why the severity is Medium rather than Critical. It cannot be checked here (A6, baseline §1) and the codebase does not rely on it explicitly anywhere — no comment at `keygen.rs:27` or `:433` says "upstream redacts". A security property that depends on an unstated upstream behaviour and would silently regress on a dependency bump is a finding in its own right, independent of today's answer.
- **"The signer private key leaks too."** It does not. `crates/core/src/tx/signer.rs:96-100` hand-writes `Debug` to print the address only, and the core analysis' rejected list records the same (`analysis-core.md:503`). Checked and excluded.
- **"The signing nonces leak through `Resume::Nonce`."** They do not: `Resume::Nonce` carries `Box<Nonces>`, and `Nonces` redacts `signing_nonces` (basis row 8). `NonceChunk` likewise has a manual impl at `preprocess.rs:150`. This is the evidence that the convention exists and that `Secrets`/`KeyShare` are the two places it was not applied.
- **"The metrics leak."** Checked separately and they do not: every label in `crates/core/src/metrics.rs`, `crates/validator/src/metrics.rs` and `crates/sentinel/src/metrics.rs` is a `&'static str` produced by a `label` method on a closed enum. The single non-static label is `"method" => method.to_owned` (`crates/core/src/metrics.rs:33`), whose value is `request.method` from an outgoing alloy RPC request (`crates/core/src/provider/mod.rs:71`) — a closed set chosen by this codebase, so neither secret-bearing nor unbounded in cardinality.
- **"The engine returns a secret over HTTP."** It does not. The engine holds no keys, and both extractor rejections are `&'static str` (`crates/sentinel-engine/src/api/extractors.rs:22-25`, `:43-46`); the only response body is `Json<Verdict>`.
- **`error!`/`warn!` on error types.** `InternalError` (`validator/service/effect.rs:259-275`) stores `err.to_string`; the reachable `Display` is `frost::Error` (`crates/validator/src/frost/error.rs:10`, `:16`), whose payload is a `frost_secp256k1::Error` variant and an `Address`. No secret field is formatted by this repo's own code.
- **Ownership note.** The single-site defect at `validator/service/effect.rs:249` is VAL-H10 and belongs to R4/R6; this finding is filed cross-cutting because the generic `core` sites (`effects.rs:55`, `:59`, `:78`, `driver.rs:238`, `:261`) log _whatever_ the downstream service puts in its `Effect`/`Resume`, which is a workspace-level contract, not a validator bug.

## Remediation options

1. Hand-write a redacting `Debug` for `Secrets` and `KeyShare` the way `EncryptionKey` and `Nonces` already do — print the group/participant identifiers and `"<redacted>"` for the share. Cheapest, entirely local, and removes the dependency on upstream behaviour. Tradeoff: debugging a DKG failure loses a field that was probably never readable anyway.
2. Make the contract explicit in `core`: document on `EffectHandler` that `Effect` and `Resume` `Debug` output is logged verbatim at `trace`, and that secret-bearing variants must redact. Optionally replace the three `?effect`/`?resume` sites with a narrower trait (`fn label(&self) -> &'static str`, which the validator already has as `Effect::metric_kind`) so `core` logs a discriminant instead of the payload.
3. Add a `zeroize`-style newtype (`Redacted<T>`) used by every secret field, so the property is enforced by the type system rather than by remembering.

Tests to add: a unit test per secret type asserting `format!("{:?}", value)` contains neither the scalar bytes nor a plausible hex run — cheap, and it fails loudly if a future dependency bump changes upstream `Debug`.

## Trail

- Reviewer R10: drafted, self-estimate 62% (the log sites, the derive/no-derive split and the default level are all E2 from direct reads; the "a secret is actually printed" leg is `I` and unresolvable this run per A6)

## Critic (C-XC)

### Per-claim verdicts

| # | Verdict | Note |
| --- | --- | --- |
| 1 | **Supported** | `effects.rs:55` `tracing::trace!(?effect, "spawning effect task");`, `:59` `tracing::trace!(?resume, "effect task finished");` — both verbatim at the cited lines. |
| 2 | **Supported** | `effects.rs:78` `tracing::trace!(?resume, "effect resume collected");`. |
| 3 | **Supported** | `validator/src/service/effect.rs:249` is exactly `tracing::warn!(?effect, %err, "failed to perform effect");`, inside the `Err` arm of `perform_effect`. |
| 4 | **Supported** | `core/src/observability/mod.rs:29-37` — `log_filter: EnvFilter::new("info")`. Confirmed by reading the whole `impl Default`. |
| 5 | **Supported** | `frost/keygen.rs:27-32` verbatim; `Secrets` holds `encryption_key: EncryptionKey` (which _does_ redact), `secret_package: round1::SecretPackage` and `proof_of_knowledge: Signature`. Worth stating precisely: the **only** unredacted secret inside `Secrets` is the `frost-core` field. |
| 6 | **Supported** | `frost/keygen.rs:432-435` — `#[derive(Clone, Debug, Deserialize, Serialize)] #[serde(transparent)] pub struct KeyShare(keys::KeyPackage);`. Because it is a newtype derive, `KeyShare`'s `Debug` _is_ `KeyPackage`'s. |
| 7 | **Supported** | `frost/ecdh.rs:50-54` verbatim. |
| 8 | **Supported** | `frost/preprocess.rs:64-70` verbatim; `NonceChunk` likewise at `:150-159`. |
| 9 | **Supported** | Baseline quote accurate; correctly classed `I`. |

No claim is marked `H`. Every citation in this file is accurate at the stated line.

### What I add: the reachability of each sink is not uniform, and the finding under-states one half

I traced the payload types myself before reading the argument. `validator/src/service/effect.rs:24-62` defines `Effect` with two variants that carry key material — `StartNonceGeneration { key_share: Arc<KeyShare> }` and `ReconcileGroupSecrets { groups: BTreeMap<B256, Option<Arc<KeyShare>>> }` — and `Resume` (`:78-102`) with `Setup { secrets: Box<Secrets> }`. So:

- the **`warn!` sink (`effect.rs:249`) needs no operator action at all.** It fires at the shipped `log_filter = "info"` (all three samples), on any `try_perform_effect` error. `ReconcileGroupSecrets` resolves to writes against the secret store, so a single SQLite failure — a locked file during the backup `docs/validator-handbook.md:75` tells operators to take, or a full disk — formats every live `Arc<KeyShare>` through `Debug`. That is a concrete, in-scope trigger.
- the **`trace!` sinks require the operator to raise the level.** That is a real but weaker path.

This asymmetry is why my number differs from C-CORE-B's 50% on `F-CORE-036`: that finding's claim is confined to the five `trace!` sinks, and 50% is right for it. This finding's claim additionally covers a sink that is live in the default configuration, which is a materially stronger mechanism.

I also re-checked the finding's own rejected list and found it sound: `Signer` redacts (`tx/signer.rs:96-100`, address only), `Nonces`/`NonceChunk` redact, no `expect` string interpolates a value (I read all 15 — see F-XC-001's Critic section), and the engine's extractor rejections are `(StatusCode, &'static str)`.

### Canonical file

Three files describe overlapping parts of this: `F-CORE-036` (R2), `F-VAL-062` (R6) and this one. They are not one defect but two, plus a union:

- **`F-VAL-062` is canonical for the remediable defect** — `Secrets` and `KeyShare` derive `Debug` while every sibling secret type in the same crate hand-redacts, and they reach a sink. The fix is two `impl Debug` blocks in `crates/validator/src/frost/keygen.rs`, and it closes the default-level `warn!` path outright.
- **`F-CORE-036` stays open as a separate finding** for the workspace contract: `core` _requires_ `Debug` on `Effect`/`Resume` and logs them unconditionally at five sites, so the safety of every present and future service is delegated downstream with no gate. Different remediation (narrow the trait to a `label` discriminant, or document the contract), so it must not be merged away.
- **This file (`F-XC-002`) is the union of the two** and introduces no third defect. Keep it for its trail and its rejected-hypothesis list, which is the most complete of the three, but the report should count one validator defect and one core-contract defect, not three.

C-CORE-B reached the same structural conclusion from the other side ("not a duplicate of VAL-H10"). The cross-references still need adding to `F-CORE-036` and `F-VAL-062`; I have not edited those files because they are other Critics' assignments and one is in progress.

### Exactly what QA must read to settle the redaction question

The open leg is one question with a one-command answer once a toolchain exists. In priority order:

1. **`frost-core 3.0.0`** (checksum `81ef2787af391c7e8bedc037a3b9ea03dde803fbd93e778e6bb369547800e5cd`, `Cargo.lock:2089-2092`), after `cargo fetch`, at `~/.cargo/registry/src/index.crates.io-*/frost-core-3.0.0/src/`. Read the `Debug` implementation — derived or hand-written — for, in order of importance:
   - `keys::SigningShare` (the scalar itself; this is the value that must not print),
   - `keys::KeyPackage` (reached by `KeyShare`'s newtype derive),
   - `keys::dkg::round1::SecretPackage` (reached by `Secrets`'s derive; check its `coefficients` field specifically),
   - `keys::SecretShare` and `round1::SigningNonces`, for completeness. Note that `frost-core` depends on `zeroize`/`zeroize_derive` (`Cargo.lock:2100-2105`), which is evidence the crate thinks about secret handling but says nothing about `Debug`.
2. **The `internals` feature**, which `crates/validator/Cargo.toml` enables — check whether it changes any of those `Debug` impls or exposes additional types.
3. **The decisive local test, which does not require reading upstream at all** and is the answer QA should actually produce: in `crates/validator/src/frost/keygen.rs`, add `#[test] fn key_share_debug_is_redacted` asserting that `format!("{:?}", KeyShare::dummy)` contains no 64-character hex run, and the equivalent for `Secrets`. That converts this leg from `I` to `E1` in one `cargo test -p validator`, independently of what upstream does today, and — as remediation option 1 notes — keeps it true across dependency bumps. This is the single highest-value follow-up in this finding.

### Finding verdict

**Confirmed** for the claim as written — which is "the repository's redaction convention has a hole and the safety of two Critical-class secrets is delegated to an upstream property nobody here can check", not "the key is printed". Both the mechanism (two derives, three sinks, one of them at the default level) and a concrete trigger (a secret-store write failure during `ReconcileGroupSecrets`) are verified against the code.

**Certainty: 74%.** `E2` throughout for everything this file actually claims; the ceiling is 89% this run and 74% reflects that the _consequence_ the reader cares about remains one unread file away.

**Severity: Medium (unchanged), and the escalation note must survive into the report.** As filed, Medium is right. But this finding is bimodal, not merely uncertain: if `frost-core` derives `Debug` on the signing share, a FROST key share is written to the validator's log at the default log level on any secret-store error, which is **Critical** by PROMPT.md §8's first bullet ("leaks or allows recovery of FROST key shares"); if it redacts, this is **Informational** hygiene. The report must not present Medium as a measured midpoint — it is a placeholder for an unanswered question, and the question is answerable for the price of one test.

## QA (QA-XC)

**Not attempted (no toolchain).** Nothing was executed: there is no `cargo`, `rustc`, `forge`, `anvil` or `just` on this machine and no dependency source on disk (A9 FALSE, `state/baseline.md` §1-2). No claim in this section is an execution claim.

**PoC written:** `rust-audit/poc/F-XC-002/` — a test module to append to `crates/validator/src/frost/keygen.rs`, plus a README giving the command, the fixtures, and what each outcome means. It has never been compiled and may need one mechanical fix (noted in the README: `secret_share` may return a reference).

**What would be run and what it would show.** `cargo test -p validator qa_xc_002 -- --nocapture`. Two assertions: that `KeyShare`'s `Debug` does not print a signing share built from the marker scalar `0xdeadbeef` in any of its three plausible renderings (hex, decimal, byte array), and that `Secrets`' `Debug` does not print the round-1 secret share read back through `secret_package.secret_share`. A green run converts this finding's only `I` leg to `E1` and drops it to Informational; a red run is a Critical FROST key-share disclosure at the shipped `log_filter = "info"`, via the `warn!` sink at `crates/validator/src/service/effect.rs:249`.

I deliberately did **not** write this as an upstream source-reading task. C-XC's item 3 is right that the local test is the answer QA should produce: it needs no registry, and unlike a source read it stays true across dependency bumps. This is question 1 of `rust-audit/poc/UNRESOLVED-DEPENDENCY-QUESTIONS.md` and, at five minutes, the highest value-per-minute item in the whole audit.

**Remediation check — at least one option is sound.** Option 1 (hand-write redacting `Debug` for `Secrets` and `KeyShare`, as `EncryptionKey` at `crates/validator/src/frost/ecdh.rs:50-53` and `Nonces` at `crates/validator/src/frost/preprocess.rs:64-70` already do) is sound and complete for the validator half: it closes the default-level `warn!` path outright and removes the dependence on an upstream property. Option 2 (narrow `core`'s generic sinks to a `label` discriminant, or document the contract) is also sound but addresses a **different** defect and must not be treated as an alternative — that is exactly C-XC's canonical split, and taking only option 2 would leave `Secrets`/`KeyShare` printable by any future sink. Option 3 (`Redacted<T>`) is sound but is a workspace-wide refactor not needed to close this. No option breaks a documented `core::state` contract (purity, at-least-once effects, undefined resume ordering are all untouched by a `Debug` impl) and none involves the Solidity reference under A7.

**Certainty: unchanged at 74%.** I add no new evidence of my own, and the `E2` ceiling of 89% applies. The bimodal escalation note in C-XC's section must survive into the report verbatim.

## Verification (V-VAL, Phase 5)

**Executed. The class-`I` leg — "whether the bytes actually appear" — is settled: they do not.**

This finding correctly declined to claim "the key is printed" and wrote itself as "the repo's own hygiene has a hole and depends on an upstream property nobody here can check". The upstream property has now been checked, by execution and by reading the pinned source. It holds.

The full evidence, the verbatim `format!("{:?}", …)` renderings of `Effect::StartNonceGeneration`, `Effect::ReconcileGroupSecrets`, `Resume::Setup` and `Resume::Nonce`, and the `frost-core` 3.0.0 source table, are in **F-VAL-062's `## Verification (V-VAL, Phase 5)` section**; they are not duplicated here. The short form:

- `SigningShare` → `SigningShare("<redacted>")` (`frost-core-3.0.0/src/keys.rs:126-133`);
- `dkg::round1::SecretPackage` → `coefficients: "<redacted>"` (`src/keys/dkg.rs:191-204`);
- `dkg::round2::SecretPackage` → `secret_share: "<redacted>"` (`src/keys/dkg.rs:337-350`);
- so `KeyShare` (plain derive over `KeyPackage`) and `Secrets` (plain derive) both render with every secret field redacted, and **nothing secret reaches `warn!(?effect, …)` at `validator/service/effect.rs:249` or any of the five `core` sinks today.**

Beware one trap if this is ever re-checked with a quick substring search: `KeyShare::dummy` seeds the signing share with `Scalar::ONE` _and_ the identifier with `Identifier::try_from(1)`, so `0000…0001` appears in the rendering as the **identifier**, which is public. A naive `contains("0000…0001")` reports a leak that is not there — it did, on the first run. Isolate the `signing_share` field before asserting.

**What survives is exactly the finding's own framing, and it is worth keeping.** The convention is applied by hand; it is applied to precisely the types whose secret lives in a local field and not to the two whose secret lives inside a `frost-core` container; and the safety of those two rests entirely on an upstream `Debug` impl that no test in this repository pins. A `frost-core` minor bump that replaced a hand-written `Debug` with a derive would turn this into a live disclosure with no signal at the boundary. That is a real hygiene defect and the remediation is unchanged.

The `core` half of the finding — that `effects.rs:40-41` requires `Debug` and then logs, imposing no constraint on what the downstream type prints, with no compile-time or review-time gate — is untouched by this run and remains as the Critic left it.

Severity **Medium / Medium → Low / Low**. Certainty **74% → 88%**: the finding is now precisely characterised in both directions. Status **Verified (reduced)**. The guard test the remediation asks for exists at `poc/F-VAL-062/debug_redaction.rs::signing_share_field_alone_is_redacted` and should be landed in-tree.
