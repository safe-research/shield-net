# F-XC-004 All three runtime images run as root, pin no base-image digest, and silently discard the build's provenance argument

| Field                | Value                                                                          |
| -------------------- | ------------------------------------------------------------------------------ |
| Status               | QA-done                                                                      |
| Crate and module     | cross-cutting: all three Dockerfiles                                           |
| Location             | `crates/validator/Dockerfile:1-37`, `crates/sentinel/Dockerfile:1-38`, `crates/sentinel-engine/Dockerfile:1-28` |
| Severity             | Low / Low                                                                      |
| Certainty            | 85%                                                                            |
| Assumptions involved | A1                                                                             |
| Tags                 | config, deps                                                                   |

## Claim

Three independent hardening gaps, identical across all three service images:

1. **No `USER`.** `debian:trixie-slim` leaves the default user as root, and none of the three
   Dockerfiles switches away from it, so every service runs as UID 0 inside its container. For
   the validator and the sentinel that process holds the signer private key and the SQLite file
   containing FROST key shares and signing nonces in plaintext (a documented A1 design choice);
   running it unprivileged costs nothing here because the binary needs no privileged port — the
   engine's default bind is 5473 and the metrics listener's is an ephemeral port — and one
   writable data directory.
2. **No digest pinning, and the two stages float independently.** The builder is `rust:1-slim`
   and the runtime is `debian:trixie-slim`; neither is pinned to a digest, so `cargo build
   --locked` reproduces the dependency graph exactly while the compiler, the C toolchain and the
   `libsqlite3-dev` the binary links against are whatever the tag resolved to that day. Because
   `rust:1-slim`'s own Debian base moves on Docker Hub's schedule and `debian:trixie-slim` is
   pinned to a release codename, the two can drift apart; the failure mode when the builder's
   base moves ahead of trixie is a runtime `GLIBC_2.xx not found` on a freshly built image, with
   no source change to explain it.
3. **The pipeline passes a build argument no Dockerfile declares.** `.github/workflows/docker.yml`
   builds each of these files with `build-args: COMMIT_SHA=${{ github.sha }}`, but no `ARG`
   statement exists in any Dockerfile in the repository. BuildKit warns and continues, so the
   published images carry no label or embedded value tying them to a commit — the one piece of
   provenance the pipeline explicitly tried to supply is dropped on the floor.

## Basis

| # | Claim | Class | Citation | Verbatim quote |
| - | ----- | ----- | -------- | -------------- |
| 1 | No `USER` (nor `EXPOSE`, nor `HEALTHCHECK`) in any Dockerfile | E2 | — | `grep -rn 'USER\|HEALTHCHECK\|EXPOSE' crates/*/Dockerfile contracts/Dockerfile` returns no match. The three files are 37, 38 and 28 lines and were read in full; the last instruction in each is `ENTRYPOINT`. |
| 2 | The runtime stage installs packages as root and copies the binary in, with no user switch | E2 | `crates/validator/Dockerfile:24-32` | ```FROM debian:trixie-slim AS runner```<br>```WORKDIR /usr/src/app```<br>```  ```<br>```RUN apt-get update && apt-get install -y --no-install-recommends \```<br>```	ca-certificates \```<br>```	libsqlite3-0 \```<br>```	&& rm -rf /var/lib/apt/lists/*```<br>```  ```<br>```COPY --from=builder /usr/src/app/target/release/validator ./validator``` |
| 3 | Both base images are floating tags | E2 | `crates/sentinel-engine/Dockerfile:5`, `:16` | ```FROM rust:1-slim AS builder```<br>```FROM debian:trixie-slim AS runner``` |
| 4 | The dependency graph *is* pinned, which is what makes the toolchain float stand out | E2 | `crates/sentinel-engine/Dockerfile:11` | ```RUN cargo build --release --locked --package sentinel-engine``` |
| 5 | The pipeline supplies `COMMIT_SHA` | E2 | `.github/workflows/docker.yml:62-70` | ```      - uses: docker/build-push-action@v6```<br>```        with:```<br>```          context: .```<br>```          file: ${{ matrix.dockerfile }}```<br>```          platforms: linux/${{ matrix.platform }}```<br>```          push: ${{ github.event_name != 'pull_request' }}```<br>```          tags: ${{ steps.meta.outputs.tags }}```<br>```          labels: ${{ steps.meta.outputs.labels }}```<br>```          build-args: COMMIT_SHA=${{ github.sha }}``` |
| 6 | No Dockerfile declares it | E2 | — | `grep -rn 'COMMIT_SHA\|ARG ' --include='Dockerfile*' .` returns no match anywhere in the repository. |
| 7 | The team applies the unprivileged pattern elsewhere, so this is an omission rather than a policy | E2 | `contracts/Dockerfile:1-3` | ```FROM ghcr.io/foundry-rs/foundry:stable```<br>```  ```<br>```COPY --chown=foundry contracts /contracts``` |
| 8 | No privileged port is needed | E2 | `crates/sentinel-engine/src/config.rs:57-59` | ```fn default_bind_address -> SocketAddr {```<br>```    ([127, 0, 0, 1], 5473).into```<br>```}``` |

## Trigger

None identified for (1) as a standalone escalation — under A1 the host and the operator are
trusted, and there is no in-container privilege boundary to cross. The value of `USER` here is
blast-radius reduction: it is the difference between a container escape or a checker bug landing
as UID 0 on the host namespace and landing as an unprivileged UID. (2) triggers on a rebuild of
an unchanged commit after Docker Hub republishes `rust:1-slim`. (3) is unconditional: every image
published by the pipeline today lacks the commit label.

## Considered and rejected

- **"`crates/core` is missing a Dockerfile."** It is not missing; `core` is a library crate and
  the map never claims one. `PROMPT.md` §4's `crates/*/Dockerfile` glob resolves to three files
  (baseline §4). Not a finding.
- **"Secrets or a config file get baked into the image."** They do not reach the *runtime* image:
  the final stage copies exactly one path, the compiled binary
  (`COPY --from=builder /usr/src/app/target/release/<bin> ./<bin>`), and the config arrives at run
  time through `ENTRYPOINT ["./<bin>"]` plus the pod spec's `--config-file` argument. The
  `ENTRYPOINT`-not-`CMD` choice is deliberate and correctly commented
  (`crates/validator/Dockerfile:34-37`). The *builder* stage is broader —
  `Dockerfile.dockerignore` is `/**` with `!/Cargo.toml`, `!/Cargo.lock`, `!/crates/**`, so
  everything under `crates/` enters the build context and the builder layer, including any real
  `validator.toml` a developer left beside the sample. That layer is not pushed, so it is
  recorded as an observation in the coverage log rather than claimed here as exposure.
- **"`ca-certificates` is unnecessary because rustls uses bundled roots."** Not asserted: the lock
  contains `webpki-roots` at 0.26.11 and 1.0.7, but which root source `reqwest 0.13` selects under
  `default-features = false, features = ["json", "rustls"]` cannot be determined without the
  dependency source (A6). Left alone.
- **"`WORKDIR /usr/src/app` would not be writable for a non-root user."** True, and it is the one
  real cost of remediation: the validator and sentinel need a writable path for the SQLite file.
  The sample configs already point that at `/var/lib/safenet/<service>/data/storage.db`
  (`crates/validator/validator.sample.toml:21`), a volume mount, so a `USER` switch plus a
  `chown` on that directory is sufficient — the image itself needs no writable location.

## Remediation options

1. Add a non-root user to each runtime stage (`RUN useradd --system --uid 10001 safenet` then
   `USER safenet`), and document that the mounted data directory must be owned by that UID.
   Tradeoff: an operator upgrading an existing deployment must chown the volume once.
2. Pin both bases by digest (`FROM rust:1.9x-slim@sha256:…`, `FROM debian:trixie-slim@sha256:…`)
   and bump them deliberately, matching the `--locked` treatment the dependencies already get. If
   full digest pinning is too much churn, pin at least the Rust minor version so builder and
   runtime cannot diverge silently.
3. Declare `ARG COMMIT_SHA` in each Dockerfile and turn it into an OCI label
   (`LABEL org.opencontainers.image.revision=$COMMIT_SHA`) in the runtime stage, so the argument
   the pipeline already passes stops being discarded.
4. Optional: add `EXPOSE 5473` to the engine image as documentation of the default bind.

Tests to add: none unit-testable. A CI step that runs `docker inspect` on the built image and
asserts a non-root `User` and a non-empty `revision` label would gate all three.

## Trail

- Reviewer R10: drafted, self-estimate 80% (every claim is a direct read of a file in
  this checkout or a grep with recorded output; the GLIBC-skew failure mode in claim 2 is
  reasoning about image behaviour, not something observable here)

## Critic (C-XC)

### Per-claim verdicts

| # | Verdict | Note |
| - | ------- | ---- |
| 1 | **Supported** | Re-ran `grep -rn 'USER\|HEALTHCHECK\|EXPOSE' crates/*/Dockerfile contracts/Dockerfile` → no match. I also read all three Dockerfiles end to end (37/38/28 lines); the last instruction in each is `ENTRYPOINT`. |
| 2 | **Supported** | `crates/validator/Dockerfile:24-32` verbatim, including the tab-indented `apt-get` continuation lines. |
| 3 | **Supported** | `crates/sentinel-engine/Dockerfile:5` `FROM rust:1-slim AS builder`, `:16` `FROM debian:trixie-slim AS runner`. Identical floating tags in the other two. |
| 4 | **Supported** | `crates/sentinel-engine/Dockerfile:11`. |
| 5 | **Supported** | `.github/workflows/docker.yml:62-70` verbatim, `build-args: COMMIT_SHA=${{ github.sha }}` on the last line. |
| 6 | **Supported** | `grep -rn 'COMMIT_SHA\|ARG ' --include='Dockerfile*' .` → no match. Confirmed independently. |
| 7 | **Supported** | `contracts/Dockerfile:1-3` — `FROM ghcr.io/foundry-rs/foundry:stable` then `COPY --chown=foundry contracts /contracts`. |
| 8 | **Supported** | `sentinel-engine/src/config.rs:57-59` verbatim. |

No claim is marked `H`. Every one of the eight rows is a direct, verifiable read and I reproduced
all three greps.

### Scope note

Claim 3's evidence lives in `.github/workflows/docker.yml`, which PROMPT.md §4 lists as out of
scope. That is fine and not a defect in the finding: the *finding* is on the in-scope Dockerfiles
(they declare no `ARG`), and the workflow is cited as reference material to establish that the
argument is actually being passed. Worth a line in the report so nobody re-litigates it.

### What I checked that the finding does not

- **The `.dockerignore` files exist.** `crates/validator/Dockerfile.dockerignore`,
  `crates/sentinel/Dockerfile.dockerignore` and `crates/sentinel-engine/Dockerfile.dockerignore`
  are each four lines (`/**`, `!/Cargo.toml`, `!/Cargo.lock`, `!/crates/**`), as baseline §4
  records. R10 handles this correctly, in the Considered-and-rejected section. **`F-VAL-064`
  (R6) states the opposite** — "there is no `.dockerignore` anywhere in the repository" — which
  is contradicted by three files in the tree. That is a counter-quotable error in another
  reviewer's finding; it belongs to the validator Critic, and I flag it here rather than editing
  their file.
- **The `COMMIT_SHA` claim is unconditional and complete.** No `ARG` exists in any Dockerfile in
  the repository, including `contracts/Dockerfile`, so every image the pipeline publishes today
  carries no revision provenance beyond the OCI labels `docker/metadata-action` adds. (Those
  labels do include a revision — but they are the action's, not the argument the workflow
  explicitly passes, which is dropped.)
- **The privileged-port argument holds for all three services**, not just the engine: the metrics
  listener defaults to an ephemeral loopback port (`core/src/observability/mod.rs:33`) and the
  engine's API to `127.0.0.1:5473`. Nothing needs `CAP_NET_BIND_SERVICE`.

### Overlap and canonical file

`F-VAL-064` (R6) covers the validator's root user, digest pinning and build context as part of a
larger claim about undetectable validator halts.

- **This file (`F-XC-004`) is canonical for the image-hardening claims** — no `USER`, no digest
  pin, the discarded `COMMIT_SHA`, and the absent `EXPOSE`/`HEALTHCHECK`. It is the only one that
  covers all three images and the only one that has the `COMMIT_SHA` claim at all.
- `F-VAL-064` should keep its distinctive and more consequential claim: `Driver::run` returning
  `` so a fatal exit yields status 0, and `/health` being unreachable under the shipped sample.
  Those are not in this file and must not be merged away.
- `F-VAL-064`'s `.dockerignore` sub-claim should be struck as counter-evidenced (above).

The cross-reference still needs adding to `F-VAL-064`; I have not edited it because it is the
validator Critic's assignment.

### Finding verdict

**Confirmed.** Mechanism and trigger both verified. For claim 3 the trigger is unconditional and
present in every published image today. For claim 2 it is a rebuild after an upstream tag moves.
For claim 1 R10 honestly writes "none identified for (1) as a standalone escalation" and files it
as blast-radius reduction — which is the correct treatment under A1 and is why this stays Low
rather than being inflated.

**Certainty: 85%.** At the top of the `E2` band; the only inference in the file is the GLIBC-skew
narrative inside claim 2, which is reasoning about image behaviour and is flagged as such in the
Trail. 89% is the run ceiling and I am not spending the last four points on a claim whose
consequence (as opposed to whose facts) is unobserved.

**Severity: Low (unchanged).** Correct under PROMPT.md §8 and A1: these are configuration and
supply-chain hardening gaps with no attacker-reachable path inside the confirmed assumptions.
Running as root is defence-in-depth, not a vulnerability, when the operator and the host are
trusted. I specifically resisted raising this: "the process holds the FROST key shares" is true
but is a consequence of A1's documented plaintext-at-rest design, not of the missing `USER`.

### Note for QA

The suggested CI gate (`docker inspect` asserting a non-root `User` and a non-empty `revision`
label) is the right shape and is cheap. Remediation 1's one real cost is correctly identified:
`WORKDIR /usr/src/app` is root-owned, so a `USER` switch needs the data directory — already a
volume mount at `/var/lib/safenet/<service>/data` per the samples — chowned to the new uid, and
the handbooks' `docker run --volume` examples would need a matching note.

## QA (QA-XC)

**Reproduced by inspection.** All three claims verified directly against this checkout. This does
**not** put the finding in the 90-100 band, which requires `E1`; nothing was executed (A9 FALSE).

- **Claim 1 (no `USER`).** `grep -n "USER" crates/*/Dockerfile` returns nothing. I read
  `crates/sentinel-engine/Dockerfile` in full: builder `rust:1-slim`, runner `debian:trixie-slim`,
  `WORKDIR /usr/src/app`, `apt-get install ca-certificates`, `COPY --from=builder`, then
  `ENTRYPOINT ["./sentinel-engine"]`. No user is set, so every process runs as uid 0.
- **Claim 2 (no digest pin).** All six `FROM` lines across the three Dockerfiles use floating
  tags — `rust:1-slim` three times, `debian:trixie-slim` three times. No `@sha256:` anywhere.
- **Claim 3 (the provenance argument is discarded).** `.github/workflows/docker.yml:70` passes
  `build-args: COMMIT_SHA=${{ github.sha }}`, and `grep -rn "ARG" crates/*/Dockerfile` returns
  **no match** — no Dockerfile declares `ARG COMMIT_SHA`, so the value is dropped and the images
  carry no record of the commit they were built from. This is the claim whose trigger is
  unconditional and present in every published image today, and it reproduces exactly as filed.

**No PoC written, deliberately.** A Rust test proving "the Dockerfile runs as root" would be
padding. The right check is the one the finding and C-XC both propose: a CI step running
`docker inspect` and asserting a non-root `User` and a non-empty revision label. That is a
pipeline change, not a test, and it belongs in `.github/workflows/docker.yml`.

**Remediation check — sound, with one real cost correctly identified.** Adding `USER` needs the
data directory chowned to the new uid: `WORKDIR /usr/src/app` is root-owned, and the samples mount
data at `/var/lib/safenet/<service>/data`, so the handbooks' `docker run --volume` examples need a
matching note or existing deployments break on upgrade. Pinning base images by digest is sound and
should be paired with an automated bump (otherwise the pin silently ages into an unpatched base,
which is worse than the floating tag). Declaring `ARG COMMIT_SHA` and emitting it as an OCI
`org.opencontainers.image.revision` label is unambiguously correct and costs one line per file.

Under A1 none of this is attacker-reachable, so **Low is right and I resisted raising it** for the
same reason C-XC did: the process holding FROST key shares is a consequence of A1's documented
plaintext-at-rest design, not of the missing `USER`.

**Certainty: unchanged at 85%.** My reads confirm the Critic's rather than adding a new class of
evidence, and the 89% ceiling stands. Question 17 of
`rust-audit/poc/UNRESOLVED-DEPENDENCY-QUESTIONS.md` (which TLS root source `reqwest` selects, and
therefore whether the `ca-certificates` install is load-bearing) is the only open item that
touches this file.
