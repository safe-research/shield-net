# F-XC-007 Dependency surface is wider than the code needs and no advisory gate exists in CI

| Field | Value |
| --- | --- |
| Status | QA-done |
| Crate and module | workspace manifests |
| Location | `Cargo.toml:6-25`, `crates/core/Cargo.toml:10-28`, `Cargo.lock:4599-4609` (related: `Justfile:22-30`, `:40-44`, `crates/core/src/tx/mod.rs:348-367`) |
| Severity | Informational / Informational |
| Certainty | 84% |
| Assumptions involved | A6, A9 |
| Tags | deps |

## Claim

**`cargo audit` was not run in this audit and no advisory status is asserted anywhere in this finding.** There is no toolchain on the review host (baseline §1, A9 false), so nothing here says anything about RUSTSEC or CVE status for any of the 516 packages in the lock. What follows is what a static read of the manifests, the lockfile and the call sites establishes.

1. **No advisory gate exists in the pipeline either.** `just check` runs `cargo fmt`, `cargo clippy --workspace --all-targets --locked -- -D warnings` and an OpenAPI lint; `just test` runs `cargo test --workspace`. There is no `cargo audit`, no `cargo deny`, no Miri, no fuzzing and no sanitizer job anywhere in `.github/workflows/` or the `Justfile`. The absence of the gate is the finding — not any particular advisory, which nobody in this run is in a position to name. For a service that holds threshold key shares and signs onchain, the distinguishing property of an advisory gate is that it fires _between_ releases, without anyone deciding to look.

2. **Two database drivers the code never uses are in the resolved graph.** `sqlx` is declared with `features = ["sqlite", "runtime-tokio"]` but without `default-features = false`, so the default `macros` feature stays on and the lock resolves `sqlx 0.9.0` with `sqlx-mysql` and `sqlx-postgres` as dependencies. The workspace uses no compile-time query macro at all — every statement is a runtime `sqlx::query(...)`, and `grep -rn 'sqlx::query!\|query_as!\| query_scalar!\|sqlx::migrate!' crates/` returns nothing — so `default-features = false` would remove both drivers and everything under them, including the third `rand` major line (`rand 0.10.1` enters solely through `sqlx-postgres`, baseline §6) and MySQL/Postgres auth primitives such as `md-5 0.11.0`.

3. **Umbrella features are enabled where the code uses one corner.** `alloy` takes `features = ["full", "json-rpc"]` and the lock accordingly contains `alloy-transport-ws`, `alloy-transport-ipc`, `alloy-signer-local` and `tungstenite 0.28.0`; the services build one HTTP provider (`core/provider/mod.rs:130-133`) and use their own `Signer` (`core/tx/signer.rs`). `tokio` takes `features = ["full"]` workspace-wide, which includes `process` — no crate spawns a subprocess.

4. **`regex` is pulled into `core` for three literal substrings.** The only use is `is_transaction_underpriced`, matching `"replacement transaction"`, `"underpriced"` and `"INTERNAL_ERROR: could not replace existing tx"` case-insensitively against an RPC error message. None contains a metacharacter, and the crate brings `regex-automata`, `regex-syntax`, `aho-corasick` and `memchr` with it. `str::to_ascii_lowercase.contains(..)` is the same behaviour.

5. **The two crypto stack questions the baseline raised both resolve cleanly, and are recorded here so no later agent re-opens them.** (a) Two SHA-256 implementations are linked in — `sha2 0.11.0` for `safenet-core` and `sha2 0.10.9` under `k256`/`frost-secp256k1` — but **no hashing-parity-critical call site is ambiguous about which it uses**: every hash that must match Solidity is `alloy::primitives::keccak256`, and the only two SHA-256 uses each name their stack by import path — `k256::sha2::Sha256` for ECDH hash-to-field and plain `sha2::Sha256` for core's HKDF. (b) The validator's own RNG does share a `rand_core` line with the FROST libraries it feeds: `rand 0.8.6`, `frost-core 3.0.0` and `frost-secp256k1 3.0.0` all depend on `rand_core 0.6.4`, and the fact that `keygen::setup` can pass its `&mut R` straight into `dkg::part1` is a compile-time proof of it. `rand 0.8` is nonetheless a superseded line (0.9 and 0.10 both exist and are both already in this lock transitively), which is a maintenance fact, not a defect.

## Basis

| # | Claim | Class | Citation | Verbatim quote |
| --- | --- | --- | --- | --- |
| 1 | CI's whole Rust gate is fmt, clippy and test | E2 | `Justfile:23-30` | `check:`<br>`    (cd contracts && test "$(forge --version \| head -1)" = "forge Version: 1.5.1-v1.5.1" && forge fmt --check && forge lint --deny notes)`<br>`    npm --prefix examples run check`<br>`    npm --prefix explorer run check`<br>`    {{prettier}} --check "**/*.md"`<br>`    cargo fmt --all -- --check`<br>`    cargo clippy --workspace --all-targets --locked -- -D warnings`<br>`    @just lint-openapi crates/sentinel-engine/openapi.yaml` |
| 2 | …and `just test`, which `ci.yml` invokes | E2 | `Justfile:41-44` | `test:`<br>`    (cd contracts && forge test -vvv)`<br>`    npm --prefix explorer run test`<br>`    cargo test --workspace` |
| 3 | No advisory/Miri/fuzz/sanitizer job exists | E2 | `.github/workflows/ci.yml:37-39` | `      - run: just deps`<br>`      - run: just check`<br>`      - run: just test` — and `ls .github/workflows/` lists only `ci.yml`, `coverage.yml`, `deploy_explorer.yml`, `docker.yml`, `gemini_review.yml`, `integration.yml`, `post_coverage_comment.yml`, `pr.yml`, `tailnet_ai_review.yml`. |
| 4 | `sqlx` keeps its default features | E2 | `Cargo.toml:19` | `sqlx = { version = "0.9", features = ["sqlite", "runtime-tokio"] }` |
| 5 | …and the lock therefore carries both unused drivers | E2 | `Cargo.lock:4599-4609` | `name = "sqlx"`<br>`version = "0.9.0"`<br>`source = "registry+https://github.com/rust-lang/crates.io-index"`<br>`checksum = "378620ccc25c62c89d8be1c819e76a88d59bdcc3304733330788948e619bfd71"`<br>`dependencies = [`<br>` "sqlx-core",`<br>` "sqlx-macros",`<br>` "sqlx-mysql",`<br>` "sqlx-postgres",`<br>` "sqlx-sqlite",`<br>`]` |
| 6 | No compile-time sqlx macro is used | E2 | — | `grep -rn --include='*.rs' 'sqlx::query!\|query_as!\|query_scalar!\|sqlx::migrate!' crates/` returns no match (exit 1). Every statement is the runtime form, e.g. `sqlx::query("CREATE TABLE IF NOT EXISTS snapshots (…)")` at `crates/core/src/state/storage.rs:50-51`. |
| 7 | Umbrella features | E2 | `Cargo.toml:6`, `:21` | `alloy = { version = "2", features = ["full", "json-rpc"] }`<br>`tokio = { version = "1", features = ["full"] }` |
| 8 | `regex` exists for three literals | E2 | `crates/core/src/tx/mod.rs:362-368` | `fn is_transaction_underpriced(err: &TransportError) -> bool {`<br>`    err.as_error_resp.is_some_and(\|payload\| {`<br>`        (iregex!("replacement transaction").is_match(&payload.message)`<br>`            && iregex!("underpriced").is_match(&payload.message))`<br>`            \|\| iregex!("INTERNAL_ERROR: could not replace existing tx").is_match(&payload.message)`<br>`    })`<br>`}` |
| 9 | The two SHA-256 stacks are disambiguated by import path | E2 | `crates/validator/src/frost/ecdh.rs:4-14` | `use k256::{`<br>`    EncodedPoint, NonZeroScalar, ProjectivePoint, Scalar,`<br>`    elliptic_curve::{`<br>`        Group,`<br>`        hash2curve::{self, ExpandMsgXmd},`<br>`        point::AffineCoordinates as _,`<br>`        sec1::{FromEncodedPoint, ToEncodedPoint},`<br>`        zeroize::{Zeroize, ZeroizeOnDrop},`<br>`    },`<br>`    sha2::Sha256,`<br>`};` |
| 10 | …and core's own SHA-256 is its own `sha2 0.11` under HKDF | E2 | `crates/core/src/kdf.rs:3-5`, `:22` | `use alloy::primitives::B256;`<br>`use hkdf::Hkdf;`<br>`use sha2::Sha256;`<br>`    let hkdf = Hkdf::<Sha256>::new(Some(domain), ikm);` |
| 11 | The validator RNG and FROST share `rand_core 0.6.4` | E2 | `Cargo.lock` (parsed) | The lock's dependency lists give `rand 0.8.6 -> rand_core 0.6.4`, `frost-core 3.0.0 -> rand_core 0.6.4`, `frost-secp256k1 3.0.0 -> rand_core 0.6.4`, alongside `elliptic-curve 0.13.8`, `ff 0.13.1`, `group 0.13.0`, `signature 2.2.0` and `crypto-bigint 0.5.5` on the same line; `rand_core 0.9.5` and `0.10.1` are confined to the `rand 0.9`/`0.10` sub-trees. Corroborates baseline §6. |
| 12 | No advisory status can be asserted | I | `rust-audit/state/baseline.md:354-356` | ``**Advisories: `cargo audit` not run** (no toolchain). No RUSTSEC or CVE status is asserted anywhere``<br>`in this baseline, for any of the 516 packages. Any dependency-advisory finding in this run is`<br>`unverifiable and must say so.` |

## Trigger

None identified — this is hardening, not a reachable defect. The nearest thing to a trigger is temporal: a vulnerability is published against any of the 516 locked packages and nothing in the pipeline notices, because nothing in the pipeline looks.

## Considered and rejected

- **"Duplicate major versions are themselves a bug."** They are not, and the 43 duplicate names the baseline counted (§6) are overwhelmingly ordinary transitive churn — `windows-*` shims, `hashbrown`, `syn`, the arkworks family. The two that would matter for correctness were checked individually and both are fine (basis rows 9-11). No finding on duplication as such.
- **"`sqlx-macros` is a proc macro, so it is not in the binary."** True of `sqlx-macros` itself, but the lock lists `sqlx-mysql` and `sqlx-postgres` as direct dependencies of the `sqlx` _lib_ (basis row 5), not only of the macro crate. Whether the linker ultimately strips the unused code is not verifiable without a build and is not claimed; the dependency-graph and build-time cost are.
- **"`alloy`'s ws/ipc transports are dead code."** Not asserted as dead — `Provider::connect` passes the configured URL to `ClientBuilder::connect`, which dispatches on scheme, so a `ws://` RPC URL is a supported configuration. The point is only that `full` is broader than `full` needs to be for what is used.
- **"`sha2 0.11` in `core` risks a hashing mismatch with Solidity."** Rejected on evidence: core's `sha2` is used at exactly one place, `kdf.rs`, whose output is the sentinel's local reveal salt (`crates/sentinel/src/hashing.rs`), never a consensus hash. Every Solidity-parity hash in the workspace is `keccak256` from `alloy::primitives`. This was the specific question the baseline asked and it is now answered.
- **`Cargo.lock` hygiene.** Checked and fine: it is committed, format version 4, and both the clippy gate and all three Dockerfiles build with `--locked`, so a resolution drift cannot slip in unnoticed.

## Remediation options

1. Add `cargo deny check` (advisories + bans + licences) or `cargo audit` as a job in `ci.yml`, scheduled daily as well as on PR so it fires between releases. Tradeoff: an advisory with no available fix turns CI red; `deny.toml` supports time-boxed exceptions for exactly that.
2. Set `sqlx = { version = "0.9", default-features = false, features = ["sqlite", "runtime-tokio"] }`. Zero code change is required because no compile-time macro is used (basis row 6); verify by building. This removes two database drivers and one `rand` major line from every shipped binary.
3. Narrow `alloy` from `full` to the transports and types actually used, and `tokio` from `full` to the runtime features each crate needs. Mechanical, and the compiler names anything missing.
4. Replace the three `iregex!` calls with `str` comparisons and drop `regex` from `crates/core/Cargo.toml`.
5. Record in `AGENTS.md` which crypto stack each SHA-256 call site belongs to, so the property verified in basis rows 9-10 stays true as call sites are added.

Tests to add: none. Items 2-4 are validated by the build and the existing suite; item 1 is the gate itself.

## Trail

- Reviewer R10: drafted, self-estimate 82% (manifests, lockfile blocks, Justfile, workflow files and every cited call site were read directly and the greps are reproduced above; no advisory claim is made, per baseline §6)

## Critic (C-XC)

### The check this finding exists to survive: does it assert any advisory status?

**No. The file is clean, and I checked mechanically rather than by impression.**

`grep -n -i -E "RUSTSEC|CVE-|vulnerab|advisor|exploit|patched|yanked|unmaintained|outdated"` over the whole file returns twelve lines. Every one of them is one of:

- the **process** claim ("no advisory _gate_ exists in CI" — the title, §1, basis row 3),
- an explicit **disclaimer** (the bolded opening sentence, basis row 12, the Trail line),
- a **remediation** ("add `cargo deny check` ... as a job"), or
- a **hypothetical** with no subject ("a vulnerability _is published_ against any of the 516 locked packages and nothing in the pipeline notices") — which is a statement about the pipeline, not about any package.

There is **no RUSTSEC identifier, no CVE identifier, and no claim anywhere that any pinned version is or is not vulnerable**. Basis row 12 pre-emptively classes the whole question `I` and cites baseline §354-356 for it. This is exactly the discipline the run requires, and had it been breached the claim would have been `H` and the finding Unsupported.

The single nearest approach is item 5's "`rand 0.8` is nonetheless a superseded line (0.9 and 0.10 both exist and are both already in this lock transitively)". That is a version-currency fact read straight off `Cargo.lock` — all three lines are in the file — and R10 immediately labels it "a maintenance fact, not a defect". It is not an advisory claim and I am recording it here only so the report can say the file was checked at that granularity.

### Per-claim verdicts

| # | Verdict | Note |
| --- | --- | --- |
| 1 | **Supported** | `Justfile:22-30` verbatim, including `cargo clippy --workspace --all-targets --locked -- -D warnings`. |
| 2 | **Supported** | `Justfile:40-44` verbatim. |
| 3 | **Supported** | `ci.yml:37-39` verbatim; the workflow listing is accurate (nine `.yml` files plus a `README.md` the row omits, which is immaterial). I re-ran `grep -rn "audit\|deny\|miri\|fuzz\|sanitiz" .github/workflows/ Justfile`, discounting `forge lint --deny notes`: **no match**. The absence is confirmed independently. |
| 4 | **Supported** | `Cargo.toml:19` verbatim — `features = ["sqlite", "runtime-tokio"]` with no `default-features = false`. |
| 5 | **Supported** | I read `Cargo.lock:4598-4609` directly rather than accepting the parse: the `sqlx 0.9.0` block lists `sqlx-core`, `sqlx-macros`, `sqlx-mysql`, `sqlx-postgres`, `sqlx-sqlite`. Quote is exact. |
| 6 | **Supported** | Grep reproduced; no compile-time macro anywhere. |
| 7 | **Supported** | `Cargo.toml:6` and `:21` verbatim. |
| 8 | **Supported** | `core/src/tx/mod.rs:361-368` verbatim; the three literals contain no metacharacter. |
| 9 | **Supported** | `validator/src/frost/ecdh.rs:4-14` verbatim, `sha2::Sha256` imported through the `k256::` path. |
| 10 | **Supported** | `core/src/kdf.rs:3-5` and `:22` verbatim. |
| 11 | **Supported** | See the lockfile audit below — I verified this against the file, not the parse. |
| 12 | **Supported** | Baseline quote exact; correctly `I`. |

No claim is marked `H`.

### Closing R10's second admitted gap: `Cargo.lock` was parsed, not read

R10's coverage log flags that `Cargo.lock` (6,169 lines) was processed programmatically. I read the blocks that matter by eye and cross-checked every dependency edge the crypto claims rest on:

| Block | Lines | What the file actually says | Matches the parse? |
| --- | --- | --- | --- |
| `frost-core 3.0.0` | 2089-2107 | deps include `rand_core 0.6.4`, `zeroize`, `zeroize_derive` | yes |
| `frost-rerandomized 3.0.0` | 2110-2121 | `rand_core 0.6.4` | yes |
| `frost-secp256k1 3.0.0` | 2124-2134 | `frost-core`, `frost-rerandomized`, `k256`, `rand_core 0.6.4`, `sha2 0.10.9` | yes |
| `rand 0.8.6` | 3645-3654 | `rand_chacha 0.3.1`, `rand_core 0.6.4` | yes |
| `rand 0.9.4` / `rand 0.10.1` | 3656-3676 | `rand_core 0.9.5` / `rand_core 0.10.1` — disjoint sub-trees | yes |
| `rand_core 0.6.4` | 3698-3706 | `getrandom 0.2.17` | yes |
| `sha2 0.10.9` / `0.11.0` | 4455-4476 | `digest 0.10.7` / `digest 0.11.3` | yes |
| `k256 0.13.4` | 2913-2926 | `sha2 0.10.9` | yes |
| `hkdf 0.13.0` → `hmac 0.13.0` | 2465-2492 | `hmac 0.13.0` → `digest 0.11.3` | yes |
| `sqlx 0.9.0` | 4598-4609 | the five sub-crates in basis row 5 | yes |

**Zero discrepancies.** The two questions the baseline raised are settled as R10 states them: the validator's `rand 0.8.6` and the three FROST crates all sit on `rand_core 0.6.4`, so `keygen::setup` handing its `&mut R` to `dkg::part1` is type-compatible for the right reason and not by coincidence; and the two SHA-256 stacks are cleanly separated by import path, with core's `sha2 0.11` reaching only `kdf.rs`. R10's programmatic parse is trustworthy for these packages, and the gap in the coverage log is closed.

### Finding verdict

**Confirmed.** Every item is a direct read of a file in this checkout, I reproduced all four greps and the lockfile blocks, and the claim is scoped to exactly what a static read can establish.

**Certainty: 84%.** Near the top of the `E2` band. The four points held back are for the two consequences that are genuinely not observable here: whether the linker strips the unused `sqlx` drivers from the shipped binaries (R10 explicitly does not claim it does), and whether narrowing `alloy`/`tokio` compiles cleanly. Both are build questions and both are unreachable this run.

**Severity: Informational (unchanged).** Correct. The Trigger honestly says "none identified — this is hardening, not a reachable defect", and PROMPT.md §8 puts hardening and test/process gaps in exactly this band. I considered raising it to Low on the strength of "a service holding threshold key shares has no advisory gate at all" and decided against: severity in this report measures the defect, not the discipline, and there is no defect here to reach.

### Note for QA and for the report

This finding should be the report's standing note that **no dependency-advisory work was performed in this audit at all**. It is not a clean bill of health for the 516 locked packages; it is the statement that nobody looked, because nobody could. Remediation 1 (`cargo deny check`, scheduled daily as well as on PR) is the item that turns that from a permanent gap into a one-off one, and it is the highest-value line in the file even though the finding is Informational.

Remediation 2 (`sqlx` `default-features = false`) is safe to attempt because basis row 6 proves no compile-time macro is used, but it must be validated by a build — the `macros` feature also gates `sqlx::FromRow` derives, and `grep -rn 'FromRow' crates/` should be run before the change is made.

Addendum (C-XC): I ran that check — `grep -rn "FromRow\|derive(sqlx" crates/ --include='*.rs'` returns no match, so nothing in the workspace uses the `macros` feature for derives either. Remediation 2 has no known code dependency; it still needs a build to confirm.

## QA (QA-XC)

**Reproduced by inspection**, for the one check this finding exists to survive. Nothing was executed (A9 FALSE) — in particular `cargo audit` was **not** run, here or anywhere in this audit.

**The advisory check, done independently.** I re-ran C-XC's grep verbatim rather than accepting its result:

```
grep -n -i -E "RUSTSEC|CVE-|vulnerab|advisor|exploit|patched|yanked|unmaintained|outdated" \
  rust-audit/findings/F-XC-007.md
```

Every hit is one of: the process claim in the title and §1 ("no advisory **gate** exists in CI"); an explicit disclaimer (the bolded opening, basis row 12, the Trail line, and C-XC's own §"The check this finding exists to survive"); a remediation proposal ("add `cargo deny check` … as a job"); or a subject-less hypothetical ("a vulnerability _is published_ against any of the 516 locked packages and nothing in the pipeline notices"), which is a statement about the pipeline and names no package. **There is no RUSTSEC identifier, no CVE identifier, and no claim anywhere that any pinned version is or is not vulnerable.** Confirmed clean, at the granularity C-XC recorded, including the nearest approach ("`rand 0.8` is a superseded line"), which is a version-currency fact read off `Cargo.lock` and is immediately labelled a maintenance fact rather than a defect.

Had this file contained an advisory claim it would have been `H` and the finding Unsupported. It does not.

**No PoC written.** There is nothing here a test can decide: items 2-4 are validated by a build, item 1 _is_ the gate. The two open questions are 9 and 19 of `rust-audit/poc/UNRESOLVED-DEPENDENCY-QUESTIONS.md` — running the advisory scan at all, and whether the linker strips the unused `sqlx` drivers from the shipped binaries. Both are recorded there with commands.

**Remediation check — sound.** Option 1 (`cargo deny check` scheduled daily as well as on PR, so it fires _between_ releases) is the highest-value line in this Informational finding and is the only item that turns a permanent gap into a one-off one. Option 2 (`sqlx` with `default-features = false`) has no known code dependency — I re-ran `grep -rn "FromRow\|derive(sqlx" crates/ --include='*.rs'`, which returns no match, confirming C-XC's addendum that nothing uses the `macros` feature for derives — but it still needs a build to confirm, and question 19 is how to tell whether it reduces the shipped surface or only the build. Options 3-5 are sound and mechanical.

**One thing this finding must carry into the report, unchanged:** it is the standing note that **no dependency-advisory work was performed in this audit at all**. It is not a clean bill of health for the 516 locked packages; it is the statement that nobody looked, because nobody could.

**Certainty: unchanged at 84%.** The two points held back are build questions (linker stripping, whether the narrowed `alloy`/`tokio` features compile), and neither is reachable this run.

## Verification (V-XC, Phase 5)

**The original text above is unchanged and remains correct.** It was filed in a run with no toolchain and it asserted, deliberately and in bold, that no advisory status was being claimed for any of the locked packages. That restraint was right. This section records what happened when the gate it asked for was finally run, and it does not soften a single sentence above.

### The process claim is vindicated by execution

`cargo audit` 0.22.2 now exists on the host and has been run. It **exits 1**: 4 vulnerabilities and 11 warnings (`rust-audit/state/logs/cargo-audit.txt`). The finding's central claim — that there is no `cargo audit`, no `cargo deny`, no Miri, no fuzzing and no sanitizer anywhere in `.github/workflows/` or the `Justfile`, and that "the absence of the gate is the finding" — is now demonstrated rather than argued. Had the gate existed, the pipeline would have been red, and every one of those fifteen entries would have been triaged when it landed rather than in Phase 5 of an audit.

**Full advisory triage is `F-XC-011`**, filed this phase. Its short form: exactly one advisory (`h2` RUSTSEC-2026-0258) reaches a network-facing surface; the single **7.5 HIGH** (`quinn-proto` RUSTSEC-2026-0185) is **not in any build graph and not in any binary**; the `ruint` `U256` shift advisory is latent because none of its eight affected functions is called anywhere in `crates/`; and of the four **yanked** crates only `spin` 0.9.8 is actually compiled, reaching all three binaries through `sqlx-sqlite` → `flume`.

### Item 2 of this finding — `sqlx-mysql` / `sqlx-postgres` — is **Refuted**

The finding's item 2 argues that `Cargo.toml:19` takes `sqlx` without `default-features = false` and so pulls `sqlx-mysql` and `sqlx-postgres` into the build, and question 19 of `rust-audit/poc/UNRESOLVED-DEPENDENCY-QUESTIONS.md` was raised to decide whether that reached the shipped binaries. It does not — and the premise is wrong one step earlier than the question assumed. They are not stripped by the linker; **they are never compiled at all.**

```
$ cargo tree -i sqlx-mysql    --workspace --locked
warning: nothing to print.
$ cargo tree -i sqlx-postgres --workspace --locked
warning: nothing to print.
$ ls target/debug/deps/ | grep -ci sqlx_mysql      -> 0
$ ls target/debug/deps/ | grep -ci sqlx_postgres   -> 0
```

And after `cargo build --release --workspace --locked` (exit 0):

```
$ nm -C target/release/validator       | grep -c -i "sqlx_mysql\|sqlx_postgres"   -> 0
$ nm -C target/release/sentinel        | grep -c -i "sqlx_mysql\|sqlx_postgres"   -> 0
$ nm -C target/release/sentinel-engine | grep -c -i "sqlx_mysql\|sqlx_postgres"   -> 0
```

The single `strings` hit in `validator` and `sentinel` is the `sqlx-core` error message `"database driver does not support force-dropping a database (Only PostgreSQL)"` — a string constant in the shared core, not driver code. `sqlx-mysql` and `sqlx-postgres` appear in `Cargo.lock` (`:4606-4607`, `:4675-4676`, `:4685`, `:4711`) as _optional_ dependencies of `sqlx`, and `sqlx` 0.9's default features do not activate them, so `features = ["sqlite", "runtime-tokio"]` already excludes them even with `default-features` left on.

**Consequence: remediation 2 of this finding is a no-op for binary surface.** Adding `default-features = false` to the `sqlx` line would remove two lockfile entries and nothing else — no bytes leave any binary, no attack surface shrinks. It is still worth doing as lockfile hygiene (it is the same class of cleanup as `F-XC-011` remediation 5, which reduces the phantom entries `cargo audit` reports), but it should not be presented to the team as a surface reduction. The finding's own framing of item 2 — "whether they reach the shipped binary decides whether remediation 2 is a real surface reduction or only a build-time saving" — answers itself: it is **neither**; it is a lockfile-only change.

### What I ran

`cargo audit` (manager, log preserved); `cargo build --release --workspace --locked -v` (**exit 0**, log at `rust-audit/state/logs/cargo-build-release-v.txt`); `cargo tree -i` for `sqlx-mysql` and `sqlx-postgres`; `nm -C` and `strings` over all three release binaries; artefact counts under `target/debug/deps`. No source file was modified for this section.

### Verdict

**Basis class:** the process claim (item 1) moves from E2 to **E1** — the absent gate is no longer inferred from reading `.github/workflows/` and the `Justfile`, it is demonstrated by a `cargo audit` run that would have failed CI. Item 2 moves to **Refuted (E1)**.

**Severity: Informational / Informational — unchanged.** The advisory exposure now has a home of its own in `F-XC-011` (Low); this finding remains what it always was, a process gap.

**Certainty: 84% → 92%.** Raised because item 1 is now executed, lowered from what it would otherwise be because one of the finding's four enumerated items is refuted. The remaining items (the wide dependency surface, the missing Miri/fuzz/sanitizer jobs) are still read-only claims.

**Cross-reference: `F-XC-011`** carries the per-advisory reachability, the yanked-crate assessment, and the concrete CI-gate remediation this finding asked for.
